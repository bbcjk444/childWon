<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.project">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission
        android:name="android.permission.DUMP"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project"
        android:usesCleartextTraffic="true">
        <activity android:name=".comu_add2"></activity>
        <activity android:name=".comu_add" />
        <!--
<activity android:name=".comu_add2"
            tools:ignore="Instantiatable" />
        -->
        <activity android:name=".listview" />
        <activity android:name=".calanderPage2" />
        <activity android:name=".informPage" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCVEIVgFGGoopGIdLnFS13SRGCsECclDe4" />

        <activity android:name=".albumPage" />
        <activity android:name=".albumFruit" />
        <activity android:name=".albumFlower" />
        <activity android:name=".albumPlant" />
        <activity android:name=".albumMychild" />
        <activity android:name=".FullImageActivity" />
        <activity android:name=".calanderPage" />
        <activity android:name=".c_menuPage" />
        <activity android:name=".facematchPage" />
        <activity android:name=".mainPage2" />
        <activity android:name=".myPage" />
        <activity android:name=".driverPage" />
        <activity android:name=".alarmpage" />
        <activity android:name=".InformActivity" />
        <activity android:name=".camerax" />
        <activity android:name=".albumPage2" />
        <activity
            android:name=".mapPage"
            android:label="@string/title_activity_map_page" />
        <activity
            android:name=".joinPage2"
            android:launchMode="singleInstance" />
        <activity
            android:name=".joinPage"
            android:launchMode="singleInstance" />
        <activity android:name=".mainPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="example.com"
                    android:scheme="https" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>